export const setActiveTab = (tab) => (
    {
        type: "panel/setActiveTab",
        tab
    }
);
export const setFilters = (filter) => (
    {
        type: "panel/Filter",
        filter
    }
)
